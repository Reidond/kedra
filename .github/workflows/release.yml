name: Build signed bootc updates

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'
  push:
    branches: [main]
    paths:
      - .github/workflows/release.yml
      - .github/workflows/release-target.yml
      - Containerfile
      - Cargo.toml
      - Cargo.lock
      - rust-toolchain.toml
      - crates/**
      - packages/**
      - etc/**
      - usr/**
      - home/**
      - hosts/**
      - build/**

permissions:
  contents: read

# One serialized release run publishes every enabled target. Each target keeps
# its own repository, key, signing environment, rank and stable tag.
concurrency:
  group: release-44
  cancel-in-progress: false

jobs:
  # Independent callers: a failure in one target never blocks the other.
  # No secrets are passed or inherited; each signer job reads only its own
  # protected environment's secrets inside release-target.yml.
  desktop:
    if: github.repository == 'Reidond/kedra' && github.ref == 'refs/heads/main' && vars.KEDRA_RELEASES_ENABLED == 'true'
    uses: ./.github/workflows/release-target.yml
    permissions:
      contents: read
      actions: read
      packages: write
    with:
      target: desktop
      runner: ubuntu-24.04

  utm:
    if: github.repository == 'Reidond/kedra' && github.ref == 'refs/heads/main' && vars.KEDRA_RELEASES_ENABLED == 'true'
    uses: ./.github/workflows/release-target.yml
    permissions:
      contents: read
      actions: read
      packages: write
    with:
      target: utm
      runner: ubuntu-24.04-arm
