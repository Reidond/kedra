# UTM 5.0.6 release notes: "Linux desktop rendering in Vulkan does not work due
# to lack of support for indirect draws for geometry shaders. Please configure
# your guest to use the older VirGL Gallium driver." Mesa already selects the
# virgl (virtio_gpu) GL driver; keep GTK 4 on GL instead of Venus Vulkan. Zink is
# not forced. Subject to manual UTM qualification.
GSK_RENDERER=${GSK_RENDERER:-gl}
